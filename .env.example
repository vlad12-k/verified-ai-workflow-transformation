# Local M5 platform environment.
# Copy to .env only when local overrides are needed.
# These credentials are development-only.

VAIT_ENVIRONMENT=development
VAIT_SERVICE_NAME=vait-platform
VAIT_API_PORT=58001
VAIT_POSTGRES_PORT=55438

POSTGRES_USER=vait
POSTGRES_PASSWORD=vait-local-password
POSTGRES_DB=vait
